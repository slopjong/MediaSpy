README.txt
==========

MediaSpy requires at least Qt 4.5!


How to build MediaSpy:
=====================

With MinGW/Windows:


With MSVC/Windows:


With Linux/*Nix:

* Open a shell, and go to the sources directory then:
mkdir build
cd build
cmake ..
make


MediaSpy database
=================

As MediaSpy is still in development, there may have some database adjustment over versions.
No update script is available for now, and surely none will be until version 1.0.
In case of problems, please, remove the file ~/.MediaSpy/MediaSpy.sqlite

